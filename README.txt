wdbc_data.csv is the datasets.
build_stump.m is the given weak learner.
AB.m is training part of AdaBoost.
test.m is testing part of AdaBoost.
run_me.m used function from AB.m and test.m can directly get the graph of training error and test error, suggest run this script first.
compare.m is the code using inbuild package (this script takes a lot of time, not suggest run it).

all .jpg files are matlab genrated graphs.
